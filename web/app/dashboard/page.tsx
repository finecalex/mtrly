export default function DashboardPage() {
  return (
    <main className="mx-auto min-h-screen max-w-5xl px-8 py-12">
      <div className="font-mono text-xs uppercase text-muted">mtrly / creator / dashboard</div>
      <h1 className="mt-4 text-4xl font-semibold">Dashboard</h1>
      <p className="mt-3 text-muted">Live earnings stream, per-URL breakdown, withdraw — Phase 7.</p>
    </main>
  );
}
