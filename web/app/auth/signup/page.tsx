export default function SignupPage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-md flex-col justify-center px-8">
      <div className="font-mono text-xs uppercase text-muted">mtrly / auth / signup</div>
      <h1 className="mt-4 text-3xl font-semibold">Create account</h1>
      <p className="mt-3 text-muted">Email + password signup + Circle wallet provisioning lands in Phase 1.</p>
    </main>
  );
}
